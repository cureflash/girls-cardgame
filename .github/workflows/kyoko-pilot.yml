name: homura-plus3-roundrobin

on:
  push:
    branches:
      - analysis/homura-plus3-roundrobin

jobs:
  roundrobin:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Run 100 games per pair
        env:
          GAMES: 100
        run: node tools/homura-plus3-roundrobin.mjs
